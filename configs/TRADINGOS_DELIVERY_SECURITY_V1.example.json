{
  "schema": "tradingos.delivery.security_config.v1",
  "version": 1,
  "mode": "DISABLED",
  "deploy_permission": "DENY",
  "adapter_id": "telegram-primary",
  "credentials": {
    "telegram_bot_token_env": "TRADINGOS_TELEGRAM_BOT_TOKEN",
    "callback_hmac_secret_env": "TRADINGOS_CALLBACK_HMAC_SECRET"
  },
  "destination_bindings": {},
  "rate_limits": {
    "delivery_attempts_per_minute": 3,
    "callbacks_per_minute": 10
  },
  "callback_max_age_seconds": 300,
  "notes": [
    "No secret or destination value belongs in this file.",
    "To enable an adapter, add an allowlisted alias with destination_env and destination_sha256, switch mode to ENABLED, and separately grant deploy_permission=ALLOW.",
    "Callback HMAC is adapter authentication; oi1 callback checksum remains integrity/dedupe only."
  ]
}
